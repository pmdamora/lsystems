% LsysExpand: Expand an axiom string using the grammar given in rules for
% nReps iterations
% function [lsys] = LsysExpand(rules, axiom, nReps)
%
% INPUTS:
% rule == a structure of 
% delta == scalar incremental angle size in Degrees
% lsys == string containing Turtle Graphics instructions
% len == base length for terminal symbols (len must be <= 1)
% (if scalar, applies to all; if vector, one element per unique terminal symbol)
%
% OUTPUTS:
% no output variables
% direct output of 2D plot of string interpreted with Turtle Graphics
%
% Sample test call: LsysDraw('FF[-F[-B]+B]+F[-B]+B',27.5,1)
% Original code at http://courses.cit.cornell.edu/bionb441/LSystem/index.html
% Modified by David Cohen & Paul D'Amora
% CS 302: Homework1A
% February 2, 2017

function [lsys] = LsysExpand(rules, axiom, nReps)
    lsys = axiom;
    cellRules = struct2cell(rules');
    for i=1:nReps
        % One-liner to perform a regex replacement on the entire string
        lsys = regexprep(lsys,cellRules(1,:),cellRules(2,:));
    end
end